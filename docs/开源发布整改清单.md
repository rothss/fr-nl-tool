# 开源发布整改清单

## 文档目的

这份文档用于指导 `opm-nl-report-query` 仓库在发布到 GitHub、并作为 OpenClaw skill 公开使用前的整改工作。

目标是让任何接手者在不了解前序对话的情况下，也能快速判断：

1. 当前哪些问题会阻断开源发布。
2. 每个问题具体落在哪些代码或文档位置。
3. 需要改到什么程度才算完成。
4. 应该按什么顺序推进。

## 当前结论

当前仓库：

1. 可以作为 GitHub 开源仓库公开。
2. 暂不建议作为“外部用户 clone 后即可使用”的公共 OpenClaw skill 直接发布。
3. 更适合先定位为“OPM/FineReport 领域参考实现”或“带业务示例的 skill 模板”。

核心原因：

1. 默认测试未全绿。
2. 仍存在个人机器路径和私有工具链依赖。
3. 文档对外部用户的可执行性不足。
4. 大量逻辑和示例仍强绑定 OPM 业务上下文。

## 使用方式

建议按优先级分三批处理：

1. P0：阻断发布，必须先修。
2. P1：强烈建议发布前修。
3. P2：可在发布后继续演进。

---

## P0 阻断发布项

### P0-1 `runner` contract 行为与测试预期不一致

#### 问题描述

`runner.py` 对结构化失败场景的返回不稳定，至少已有一个测试失败，说明“稳定 skill contract”尚未收口。

#### 证据

失败命令：

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
```

失败用例：

- `tests/test_runner_contract.py:212`
- 断言位置：`tests/test_runner_contract.py:249`

失败现象：

- 期望：`route_not_found_in_report`
- 实际：`no_report_match`

相关代码位置：

- `scripts/runner.py:270`
- `scripts/runner.py:312`
- `scripts/runner.py:245`
- `scripts/data/source_loader.py:134`
- `scripts/adapters/openclaw_contract.py:20`

#### 需要整改什么

1. 明确 `runner.run_query()` 在以下场景下的统一返回策略：
   - 没有报表候选
   - 有报表候选，但本地文件 route/date/schema 不匹配
   - 需要 live refresh，但 refresh 未发生或失败
2. 确保 `wrap_legacy_result()` 不会把更具体的预检失败原因覆盖成更泛化的 `no_report_match`。
3. 如果测试预期是正确的，则修代码；如果产品语义已变，则同步更新测试和文档，但必须统一。

#### 验收标准

1. 下面命令全绿：

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
```

2. `tests/test_runner_contract.py` 中对以下 reason code 的断言全部稳定通过：
   - `no_report_match`
   - `route_not_found_in_report`
   - `date_not_found_in_report`
   - `report_schema_invalid`
   - `live_refresh_failed`
3. `scripts/runner.py` 入口的失败语义在 `SKILL.md` 或 `README.md` 中有一处明确说明。

#### 建议完成后复查

1. 检查 `tests/test_openclaw_contract.py`
2. 检查 `tests/test_source_loader.py`
3. 检查 `tests/test_e2e_contract.py`

---

### P0-2 清除个人机器绝对路径和私有环境默认值

#### 问题描述

仓库仍包含多个作者本机绝对路径或仅作者机器可用的默认值。对外开源后，这会造成：

1. 用户直接照文档执行会失败。
2. 仓库看起来像个人工作区快照，而非可公开复用项目。
3. 可能暴露不必要的个人环境结构。

#### 证据位置

文档中的绝对路径：

- `SKILL.md:15`
- `SKILL.md:21`
- `SKILL.md:62`
- `SKILL.md:70`
- `MILESTONE_TECH_DESIGN_REFACTOR.md:107`

代码中的绝对路径或个人默认值：

- `scripts/export_report_live.ps1:5`
- `scripts/export_report_live.ps1:7`
- `scripts/export_report_live.ps1:9`
- `scripts/export_future_kzl_live.mjs:12`
- `tests/test_offline_query_integration.py:20`

其他个人目录痕迹：

- `TODO.md:64`
- `TODO.md:65`
- `TODO.md:66`

#### 需要整改什么

1. 所有公开文档中的调用命令改为相对路径。
2. 所有脚本中的默认路径改为：
   - 环境变量控制
   - 相对路径
   - 或空值并在运行时报错
3. 不允许保留 `C:\Users\...` 作为 runtime fallback。
4. 测试中若必须引用本地镜像路径，改为环境变量驱动。

#### 建议改法

1. `SKILL.md` 中全部改为：

```powershell
python scripts/runner.py "..." --output-format json
```

2. `scripts/export_report_live.ps1`：
   - `OpmSkillRoot` 改为显式参数或空默认值
   - `EdgeProfileDir` 改为可选参数或相对目录
   - `FallbackEdgeProfileDir` 同上
3. `scripts/export_future_kzl_live.mjs`：
   - `outputFile` 不允许 fallback 到 `C:/Users/...`
   - 仅使用 `FR_MIRROR_ROOT` 推导或要求显式传参
4. `tests/test_offline_query_integration.py`：
   - 改为读取 `FR_TEST_MIRROR_ROOT`
   - 不存在则跳过

#### 验收标准

1. 下面搜索无结果：

```powershell
rg "C:\\Users\\" .
```

2. 下面搜索仅允许出现在历史审计文档或明确保留的 docs 中；不允许出现在可执行脚本和面向用户文档中：

```powershell
rg "ZhuanZ|zhuanz|Zhuann\\Z" .
```

3. `SKILL.md` 和 `README.md` 的所有命令都能在仓库根目录直接执行，不依赖作者本机路径。
4. 所有 live/export 脚本在未配置外部依赖时，报错信息明确且不引用作者环境路径。

---

### P0-3 将私有工具链依赖改为可选能力，而不是默认前提

#### 问题描述

当前完整链路依赖至少两个仓库或环境能力：

1. `finereport-search`
2. `opm-incremental-download`

如果不拆解，对外用户将无法理解“基础模式能做什么，增强模式又需要什么”。

#### 证据位置

- `.env.example:14`
- `scripts/query_fr_nl.py:212`
- `scripts/export_components_and_index.py:224`
- `scripts/export_report_live.ps1:31`
- `scripts/export_report_live.ps1:33`

#### 需要整改什么

1. 定义两种运行模式：
   - 基础模式：本地已有镜像和 catalog 时，可执行纯离线查询
   - 增强模式：支持 live refresh、组件抓取、增量索引
2. 未安装外部工具时：
   - 基础模式仍可运行
   - 增强模式返回明确错误说明缺失什么
3. 文档中明确：哪些功能依赖外部工具、哪些不依赖。

#### 验收标准

1. 未配置 `FR_BUILD_INDEX_PY` 时，离线查询基础链路仍可运行。
2. 未配置 `OpmSkillRoot` 或等价外部下载器时，调用 live refresh 会返回结构化失败，不会出现难以理解的路径错误。
3. `README.md` 有一节明确列出：
   - 必需依赖
   - 可选依赖
   - 各自对应能力
4. `SKILL.md` 不再暗示所有用户默认可用 live export。

---

## P1 强烈建议发布前整改项

### P1-1 统一项目命名、产品定位和对外描述

#### 问题描述

当前仓库存在多套并行命名：

1. `opm-nl-report-query`
2. `fr-nl-report-query`
3. `OPM NL Report Query`
4. “自然语言报表查询系统”

这会让外部用户无法判断：

1. 它是通用 FineReport skill。
2. 还是 OPM 领域专用 skill。
3. 还是一个研究原型。

#### 证据位置

- `README.md:1`
- `SKILL.md:2`
- `SKILL.md:6`
- `agents/openai.yaml:2`
- `agents/openai.yaml:3`
- `agents/openai.yaml:4`
- `LICENSE:3`

#### 需要整改什么

先做一个明确决策，并全文统一：

方案 A：领域参考实现

1. 保留 OPM 语义。
2. 首页明确写“这是 OPM/FineReport 场景下的参考实现”。
3. 不承诺通用可迁移性。

方案 B：通用 skill 模板

1. 继续去 OPM 化。
2. 把 OPM 专有内容下沉到 `examples/` 或 `profiles/`。
3. 首页定位为“可定制 FineReport NL 查询 skill”。

#### 验收标准

1. 仓库首页首屏 5 行内能清楚回答：
   - 这是做什么的
   - 面向谁
   - 能不能直接用
2. `README.md`、`SKILL.md`、`agents/openai.yaml` 中名称一致。
3. 不再出现“看起来像通用工具，实际却高度私有化”的描述偏差。

---

### P1-2 清理个人痕迹和过于具体的默认示例

#### 问题描述

仓库没有看到明显真实凭据泄露，但仍有较多个人身份和具体业务样例痕迹。它们未必敏感，但会降低公开仓库的专业度，也可能引发合规疑问。

#### 证据位置

- `references/user_scope.example.yaml:1`
- `references/user_scope.example.yaml:3`
- `references/user_scope.example.yaml:6`
- `references/user_scope.example.yaml:8`
- `tests/test_offline_query_integration.py:20`
- `SKILL.md:3`
- `SKILL.md:21`
- `references/intent_examples.json:3`
- `references/intent_examples.json:12`
- `references/intent_examples.json:16`

#### 需要整改什么

1. 将示例用户名改为通用值，例如：`demo_user`。
2. 将个人 scope 示例改为脱敏示例。
3. 对业务样例保留必要的领域说明，但避免看起来像真实生产数据摘录。
4. 如果某些航线、报表名、组织名涉及内部语义，需在发布前做一次业务侧确认。

#### 验收标准

1. `references/user_scope.example.yaml` 中不再出现真实或半真实用户名。
2. 所有默认示例都满足“公开展示也不会引起内部信息披露疑虑”。
3. 如保留具体报表名，应在 README 中写明：这些只是示例 profile，不代表通用配置。

---

### P1-3 重写 README 和 SKILL 文档，保证外部用户能正确理解可运行边界

#### 问题描述

当前文档已经有基础结构，但对外部用户仍存在误导：看起来像“快速开始后就能跑”，但实际上依赖 FineReport 环境、镜像数据、目录库、可选登录态等条件。

#### 证据位置

- `README.md:5`
- `README.md:25`
- `README.md:31`
- `README.md:53`
- `SKILL.md:10`
- `SKILL.md:57`
- `tests/README.md:71`

#### 需要整改什么

`README.md` 至少要新增以下章节：

1. Project Status
2. Who This Is For
3. What Works Offline
4. What Requires Live FineReport Access
5. Required Inputs
6. Limitations
7. Security Notes

`SKILL.md` 至少要明确：

1. 唯一入口是 `scripts/runner.py`
2. 返回的是结构化 envelope
3. 不保证无 FineReport 环境即可完整运行 live path
4. 调用方不应自行读取 Excel 兜底

#### 验收标准

1. 一个首次进入仓库的外部用户，不看代码，只看 README 就能知道：
   - 他能否直接运行
   - 如果不能，还差什么
   - 基础模式和增强模式差异是什么
2. README 的快速开始命令不再依赖隐含前提。
3. `SKILL.md` 中所有示例命令均可在仓库根目录执行。

---

### P1-4 清理 `.gitignore` 与已跟踪文件的冲突规则

#### 问题描述

`.gitignore` 当前忽略 `tests/`，但仓库已经正式跟踪 `tests/*`。这会给后续贡献者造成困惑。

#### 证据位置

- `.gitignore:36`
- `.gitignore:37`
- `.gitignore:38`
- `.gitignore:39`

#### 需要整改什么

1. 删除对整个 `tests/` 目录的忽略规则。
2. 如果只想忽略临时测试文件，精确到命名模式，不要覆盖正式测试目录。

#### 验收标准

1. `.gitignore` 中不再有会误伤正式测试目录的规则。
2. 新增测试文件时，`git status` 能正常显示。

---

## P2 可后续推进的优化项

### P2-1 将 OPM 领域逻辑与 skill 框架逻辑拆层

#### 问题描述

当前大量业务判断写死在运行时代码中，影响通用性，也增加维护成本。

#### 证据位置

- `scripts/query_fr_nl.py:47`
- `scripts/query_fr_nl.py:78`
- `scripts/query_fr_nl.py:91`
- `scripts/query_fr_nl.py:107`
- `scripts/query_fr_nl.py:135`
- `scripts/planning/router.py:13`
- `scripts/planning/router.py:34`
- `scripts/planning/router.py:53`
- `scripts/data/source_loader.py:52`
- `scripts/data/source_loader.py:58`
- `scripts/data/source_loader.py:64`

#### 需要整改什么

建议分层：

1. 通用层：
   - intent parsing
   - planning
   - source acquisition contract
   - openclaw envelope
2. 领域层：
   - 报表 family 路由
   - 报表名映射
   - live refresh 策略
   - 业务特定分析器
3. 示例层：
   - OPM profile
   - demo references

#### 验收标准

1. 未来新增一个新领域时，不需要在 `query_fr_nl.py` 中继续堆大量 `if report_name == ...`。
2. 至少路由和 refresh 策略能通过配置或 profile 模块扩展。

---

### P2-2 提供最小可公开 demo 数据和 demo 查询

#### 问题描述

目前多数“真实价值”来自本地镜像和业务报表，但这些内容外部用户拿不到，导致项目难以试用。

#### 建议新增内容

1. `examples/demo_mirror/`
2. `examples/demo_queries.md`
3. `examples/demo_user_scope.yaml`
4. 一个最小 catalog 构建说明

#### 验收标准

1. 不接 FineReport 服务，仅靠 demo 数据即可跑通至少 2 到 3 个查询。
2. README 中有明确 demo 流程。

---

### P2-3 清理根目录审计/报告型 HTML 文档

#### 问题描述

根目录当前存在较多阶段性 HTML 报告文件，会降低仓库主结构清晰度。

#### 证据位置

- `OPEN_SOURCE_FEASIBILITY_REPORT.html`
- `ARCHITECT_REVIEW_qwen3p5-27b.html`
- `PROJECT_INTRODUCTION_qwen3p5-27b.html`
- `AUDIT_REPORT_TECH_BUSINESS.html`
- `README_REVIEW.html`
- `opm_nl_report_query_status.html`

#### 需要整改什么

1. 迁移到 `docs/` 或 `docs/archive/`
2. 或只保留最必要的 1 到 2 份

#### 验收标准

1. 仓库根目录优先展示：
   - README
   - LICENSE
   - SKILL
   - scripts
   - references
   - tests
2. 阶段性分析材料不再干扰主入口阅读。

---

## 合规与发布前确认项

以下内容不是纯代码问题，但发布前应有人确认：

### C1 业务术语和报表名是否允许公开

重点检查：

- `references/report_schemas.yaml`
- `references/component_url_registry.yaml`
- `scripts/query_fr_nl.py`
- `scripts/planning/router.py`

确认内容：

1. 报表名称是否属于内部资产。
2. 目录结构、viewlet 路径、字段名是否可对外公开。
3. 航线、组织、经营类术语是否需要泛化或脱敏。

### C2 是否保留 OPM 领域示例

如果保留，则 README 首屏建议明确写：

1. 本仓库包含 OPM/FineReport 领域示例。
2. 示例用于展示 skill 设计，不保证可直接迁移到其他组织。

### C3 本地命令执行边界说明

重点位置：

- `scripts/intent/parser_llm.py:50`

说明点：

1. `FR_INTENT_LLM_COMMAND` 是本地可信命令入口。
2. 不应直接拼接不可信用户输入形成 shell 命令。
3. README 需明确这是高级可选功能。

---

## 推荐整改顺序

### 第 1 阶段：先把仓库修到“可发”

1. 修 P0-1 测试失败。
2. 修 P0-2 绝对路径和个人路径。
3. 修 P0-3 私有工具链改为可选。
4. 重跑测试。

阶段验收命令：

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
rg "C:\\Users\\" .
```

### 第 2 阶段：把文档修到“外部用户看得懂”

1. 重写 `README.md`
2. 重写 `SKILL.md`
3. 统一命名和项目定位
4. 清理 `.gitignore`

阶段验收标准：

1. 新人只看 README 能判断是否能跑。
2. 文档命令均使用相对路径。
3. 文档里不再暗示不存在的开箱即用能力。

### 第 3 阶段：把项目修到“值得别人复用”

1. 领域逻辑拆层。
2. 增加 demo 数据。
3. 迁移根目录报告文档。

---

## 发布门槛建议

满足以下条件后，再作为公开 skill 宣发：

1. 默认测试全绿。
2. 仓库中无作者个人绝对路径。
3. 私有工具链已变为可选能力。
4. README 已明确项目边界、前置条件和限制。
5. 命名和定位已统一。
6. 业务侧已确认允许公开当前术语、报表名和示例。

---

## 建议的发布定位文案

如果短期内不做彻底去领域化，建议对外定位为：

`An OpenClaw skill reference implementation for natural-language querying over FineReport-based business reports, with an OPM-focused example profile.`

对应中文：

`一个用于 FineReport 业务报表自然语言查询的 OpenClaw skill 参考实现，当前仓库内置 OPM 场景示例 profile。`

这个定位比“通用公共 skill”更符合当前代码现状。

---

## 接手提示

如果后续重新接手这项工作，优先看以下文件：

1. `开源发布整改清单.md`：总控文档。
2. `README.md`：当前公开叙述是否准确。
3. `SKILL.md`：对 OpenClaw 的契约表述是否准确。
4. `scripts/runner.py`：skill 入口 contract。
5. `scripts/data/source_loader.py`：预检与 refresh 语义。
6. `tests/test_runner_contract.py`：当前 contract 约束。
7. `scripts/export_report_live.ps1`：私有依赖最重的部分。
8. `scripts/query_fr_nl.py`：领域耦合最重的部分。

接手时第一步建议永远先执行：

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
```

然后搜索：

```powershell
rg "C:\\Users\\|ZhuanZ|zhuanz|opm-incremental-download|finereport-search" .
```

这样可以最快恢复上下文。
